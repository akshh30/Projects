const listaEl = document.getElementById('lista');
const listbEl = document.getElementById('listb');
const listcEl = document.getElementById('listc');
const listdEl = document.getElementById('listd');


const listeEl = document.getElementById('liste');
const listfEl = document.getElementById('listf');
const listgEl = document.getElementById('listg');
const listhEl = document.getElementById('listh');
const listiEl = document.getElementById('listi');
const listjEl = document.getElementById('listj');
const listkEl = document.getElementById('listk');
const listlEl = document.getElementById('listl');




function showchai() {
    if (listaEl.style.visibility === "visible") {
        listaEl.style.visibility = "hidden";
        listaEl.style.height="0px";
        listaEl.style.width="0px";
    } else {
        listaEl.style.visibility = "visible";
        listaEl.style.height="fit-content";
        listaEl.style.width="fit-content";

    }
}

function showcoffe() {
    if (listbEl.style.visibility === "visible") {
        listbEl.style.visibility = "hidden";
        listbEl.style.height="0px";
        listbEl.style.width="0px";
    } else {
        listbEl.style.visibility = "visible";
        listbEl.style.height="fit-content";
        listbEl.style.width="fit-content";

    }
}

function showBun() {
    if (listcEl.style.visibility === "visible") {
        listcEl.style.visibility = "hidden";
        listcEl.style.height="0px";
    } else {
        listcEl.style.visibility = "visible";
        listcEl.style.height="fit-content";

    }
}

function showShakes() {
    if (listdEl.style.visibility === "visible") {
        listdEl.style.visibility = "hidden";
        listdEl.style.height="0px";
    } else {
        listdEl.style.visibility = "visible";
        listdEl.style.height="fit-content";

    }
}







function showMaggie() {
    if (listeEl.style.visibility === "visible") {
        listeEl.style.visibility = "hidden";
        listeEl.style.height="0px";
    } else {
        listeEl.style.visibility = "visible";
        listeEl.style.height="fit-content";

    }
}

function showMunchies() {
    if (listfEl.style.visibility === "visible") {
        listfEl.style.visibility = "hidden";
        listfEl.style.height="0px";
    } else {
        listfEl.style.visibility = "visible";
        listfEl.style.height="fit-content";

    }
}

function showPizza() {
    if (listgEl.style.visibility === "visible") {
        listgEl.style.visibility = "hidden";
        listgEl.style.height="0px";
    } else {
        listgEl.style.visibility = "visible";
        listgEl.style.height="fit-content";

    }
}

function showPasta() {
    if (listhEl.style.visibility === "visible") {
        listhEl.style.visibility = "hidden";
        listhEl.style.height="0px";
    } else {
        listhEl.style.visibility = "visible";
        listhEl.style.height="fit-content";

    }
}

function showTornado() {
    if (listiEl.style.visibility === "visible") {
        listiEl.style.visibility = "hidden";
        listiEl.style.height="0px";
    } else {
        listiEl.style.visibility = "visible";
        listiEl.style.height="fit-content";

    }
}

function showBurger() {
    if (listjEl.style.visibility === "visible") {
        listjEl.style.visibility = "hidden";
        listjEl.style.height="0px";
    } else {
        listjEl.style.visibility = "visible";
        listjEl.style.height="fit-content";

    }
}

function showMomo() {
    if (listkEl.style.visibility === "visible") {
        listkEl.style.visibility = "hidden";
        listkEl.style.height="0px";
    } else {
        listkEl.style.visibility = "visible";
        listkEl.style.height="fit-content";

    }
}

function showSandwich() {
    if (listlEl.style.visibility === "visible") {
        listlEl.style.visibility = "hidden";
        listlEl.style.height="0px";
    } else {
        listlEl.style.visibility = "visible";
        listlEl.style.height="fit-content";

    }
}





